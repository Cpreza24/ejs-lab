<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('./partials/nav') %>
    <title>Menu</title>
</head>
<body>
    <h1>Full Menu</h1>
    <ul>
        <% RESTAURANT.menu.forEach(item => { %>
            <li>
                <strong><%= item.name %></strong> <strong>Price:</strong> <%= item.price %> <strong>Rating:</strong> <%= item.rating %> <strong>Details:</strong> <%= item.details %>
            </li>
        <% }) %>
    </ul>

</body>
</html>
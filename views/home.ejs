<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('./partials/nav') %>
    <title><%= RESTAURANT.name %></title>
</head>
<body>
    <h1>Home Page</h1>
    <p><%= RESTAURANT.name %></p>
    <p><%= RESTAURANT.address %></p>
    <p><%= RESTAURANT.phone %></p>
        <% if (RESTAURANT.isOpen) { %>
            <p>Yes we are open!</p>
        <% } else { %>
            <p>Sorry were closed</p>
        <% } %>
</body>
</html>